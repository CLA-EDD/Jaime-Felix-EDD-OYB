package miPrincipal;

public class SortingAndSearching<T extends Comparable<T>> {
    
   // Implementar algoritmo de ordenamiento por seleccion (ascendente)
    public void selectionSort (T[] data)
    {
    }

    // Implementar algoritmo de ordenamiento por insercion (ascendente)
    public void insertionSort (T[] data)
    {
    }

    // Implementar algoritmo de ordenamiento de burbuja (ascendente)
    public void bubbleSort (T[] data)
    {
    }

    // Implementar algoritmo de ordenamiento rapido (descendente)
    public void quickSort (T[] data, int min, int max)
    {
    }

    // Implementar algoritmo de ordenamiento por mezcla (descendente)
    @SuppressWarnings("unchecked")
    public void mergeSort (T[] data, int min, int max)
    {
    }
    
    // Implementar algoritmo de busqueda lineal
    // Regresar posicion del elemento buscado
    // Regresar -1 si el elemento no se encuentra
    public int linearSearch (T[] data, int min, int max, T target)
    {
      return -1;
    }

    // Implementar algoritmo de busqueda binaria
    // Regresar posicion del elemento buscado
    // Regresar -1 si el elemento no se encuentra
    public int binarySearch (T[] data, int min, int max, T target)
    {  
       return -1;
    }
 
}
